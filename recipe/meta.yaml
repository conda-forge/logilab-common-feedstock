{% set name = "logilab-common" %}
{% set version = "1.5.1" %}
{% set sha256 = "8b623c03cb61babc2fe8892715f869d1a3a9963d7525e7ef3a73cf6ff66e42e9" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  preserve_egg_dir: True
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  build:
    - python
    - pip

  run:
    - python
    - setuptools
    - six >=1.4.0

test:
  imports:
    - logilab
    - logilab.common
    - logilab.common.ureports

about:
  home: http://www.logilab.org/project/logilab-common
  license: LGPL 2.1
  license_file:
    - COPYING
    - COPYING.LESSER
  summary: 'collection of low-level Python packages and modules used by Logilab projects'

extra:
  recipe-maintainers:
    - jakirkham
